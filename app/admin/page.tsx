import { desc,eq } from "drizzle-orm"; import { ShieldCheck } from "lucide-react"; import { chatGPTSignInPath,chatGPTSignOutPath,getChatGPTUser } from "@/app/chatgpt-auth"; import { getDb } from "@/db"; import { applications } from "@/db/schema"; import { requireAdmin } from "@/lib/admin"; import ReviewButtons from "./review-buttons";
export const dynamic="force-dynamic"; export const metadata={title:"K Aura Admin"};
export default async function Admin(){const user=await getChatGPTUser();if(!user)return <main className="admin-gate"><ShieldCheck size={40}/><h1>K Aura back office</h1><p>Authorized members of Bre’s admin team can review applications and verify credentials here.</p><a href={chatGPTSignInPath("/admin")} target="_top">Sign in securely</a></main>;const admin=await requireAdmin();if(!admin)return <main className="admin-gate"><ShieldCheck size={40}/><h1>Access not approved</h1><p>{user.email} is signed in, but Bre has not added this email to the K Aura admin team.</p><a href={chatGPTSignOutPath("/admin")}>Use another account</a></main>;const rows=await getDb().select().from(applications).where(eq(applications.status,"pending")).orderBy(desc(applications.createdAt));return <main className="admin-page"><header><div><p>K Aura</p><h1>Application review</h1></div><div className="admin-links"><a href="/admin/tours">Tour inquiries</a><a href={chatGPTSignOutPath("/")}>Sign out</a></div></header><section className="admin-list">{rows.length?rows.map(a=><article className="application-card" key={a.id}><div className="application-summary"><div><span>{a.isStudent?"Restricted-hours applicant":"Licensed applicant"}</span><h2>{a.businessName||a.fullName}</h2><p>{a.fullName} · {a.profession} · {a.rentalType}</p></div><strong>{a.eligibleHours}</strong></div><div className="application-details"><p><b>Contact</b>{a.email}<br/>{a.phone}</p><p><b>Services</b>{a.services}</p><p><b>Client page</b>{a.wantsClientLink?"Requested":"Not requested"}<br/>{a.bookingUrl||"No link provided"}</p></div><div className="document-links">{a.isStudent?<><a href={`/api/admin/documents/${a.id}/id-front`} target="_blank">Review ID front</a><a href={`/api/admin/documents/${a.id}/id-back`} target="_blank">Review ID back</a></>:<a href={`/api/admin/documents/${a.id}/license`} target="_blank">Review license</a>}</div><ReviewButtons id={a.id} name={a.businessName||a.fullName}/></article>):<div className="admin-empty"><h2>No applications waiting.</h2><p>New submissions will appear here.</p></div>}</section></main>}
