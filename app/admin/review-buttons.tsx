"use client"; import { useState } from "react"; import { Check, X } from "lucide-react";
export default function ReviewButtons({id,name}:{id:number,name:string}){const [busy,setBusy]=useState(false);async function act(action:string){setBusy(true);const response=await fetch(`/api/admin/applications/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({action,slug:name})});if(response.ok)location.reload();else{alert((await response.json()).error);setBusy(false)}}return <div className="review-actions"><button disabled={busy} onClick={()=>act("approve")}><Check size={15}/>Approve</button><button disabled={busy} onClick={()=>act("reject")}><X size={15}/>Decline</button></div>}
