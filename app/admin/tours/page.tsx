import { desc } from "drizzle-orm"; import { chatGPTSignInPath,getChatGPTUser } from "@/app/chatgpt-auth"; import { getDb } from "@/db"; import { tourInquiries } from "@/db/schema"; import { requireAdmin } from "@/lib/admin";
export const dynamic="force-dynamic"; export default async function Tours(){const user=await getChatGPTUser();if(!user)return <main className="admin-gate"><h1>Tour inquiries</h1><a href={chatGPTSignInPath("/admin/tours")} target="_top">Sign in securely</a></main>;if(!await requireAdmin())return <main className="admin-gate"><h1>Access not approved</h1><p>Bre has not added {user.email} to the admin team.</p></main>;const rows=await getDb().select().from(tourInquiries).orderBy(desc(tourInquiries.createdAt));return <main className="admin-page"><header><div><p>K Aura concierge</p><h1>Tour inquiries</h1></div><a href="/admin">Applications</a></header><section className="admin-list">{rows.length?rows.map(t=><article className="application-card" key={t.id}><div className="application-summary"><div><span>{t.rentalType} inquiry</span><h2>{t.fullName}</h2><p>{t.email} · {t.phone}</p></div><strong>{t.preferredDate} at {t.preferredTime}</strong></div>{t.message&&<p>{t.message}</p>}</article>):<div className="admin-empty"><h2>No tour inquiries yet.</h2></div>}</section></main>}
