"use client";
import { FormEvent, useState } from "react";
import { Plus } from "lucide-react";
export default function EventManager(){const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const r=await fetch("/api/admin/events",{method:"POST",body:new FormData(e.currentTarget)});if(r.ok)location.reload();else{alert((await r.json()).error);setBusy(false)}}return <form className="admin-event-form" onSubmit={submit}><h2>Add a salon event</h2><div className="two-col"><label>Event title<input required name="title"/></label><label>Event type<select required name="eventType" defaultValue=""><option value="" disabled>Select one</option><option>Open Tour Day</option><option>School Visit</option><option>Charity Event</option><option>Workshop</option><option>Salon Announcement</option></select></label><label>Starts<input required type="datetime-local" name="startsAt"/></label><label>Ends <small>(optional)</small><input type="datetime-local" name="endsAt"/></label></div><label>Location<input required name="location" defaultValue="K Aura Salon"/></label><label>Description<textarea name="description" rows={3}/></label><button disabled={busy}><Plus size={17}/>{busy?"Publishing…":"Publish event"}</button></form>}
